# Low-noise default-branch guard.
# Reusable logic lives in BFlinkDesign/dev-setup.
name: guard

on:
  push:
    branches: [claude/reverse-engineer-mudmixer-NWWzn]
  delete:

jobs:
  guard:
    uses: BFlinkDesign/dev-setup/.github/workflows/guard-main.yml@main
    with:
      protected_branch: claude/reverse-engineer-mudmixer-NWWzn
    permissions:
      contents: read
      issues: write